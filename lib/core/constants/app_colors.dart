import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF6750A4);
  static const accent = Color(0xFF938F99);
  static const bgLight = Color(0xFFF7F7FA);
  static const bgDark = Color(0xFF121212);

  // Note background palette
  static const noteColors = <Color>[
    Color(0xFFFFF59D), // soft yellow
    Color(0xFFA5D6A7), // soft green
    Color(0xFF90CAF9), // soft blue
    Color(0xFFFFCCBC), // soft orange
    Color(0xFFCF93D9), // soft purple
    Color(0xFFFFFFFF), // white
  ];

  // Text color palette
  static const textColors = <Color>[
    Color(0xFF000000), // black
    Color(0xFFFFFFFF), // white
    Color(0xFF424242), // dark gray
    Color(0xFF1A237E), // indigo 900
    Color(0xFF0D47A1), // blue 900
    Color(0xFF004D40), // teal 900
    Color(0xFFB71C1C), // red 900
    Color(0xFFF57F17), // amber 900
  ];
}
